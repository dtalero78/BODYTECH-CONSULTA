{
  "name": "bsl-consultavideo",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo: backend Express + frontend Vite. Cada subdir tiene su propio package.json. Este root proxy script existe para tooling que asume convención npm en cwd (ej. coding harness).",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd backend && npm run build && cd ../frontend && npm run build",
    "lint": "cd backend && npm run lint && cd ../frontend && npm run lint",
    "lint:fix": "cd backend && npm run lint:fix && cd ../frontend && npm run lint:fix",
    "test": "cd backend && npm test"
  }
}
